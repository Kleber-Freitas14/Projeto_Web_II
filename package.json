{
  "name": "projetoaula1",
  "version": "1.0.0",
  "description": "* Node.js 22 ou superior\r Verificar se tem o node: node -v",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "watch": "tsc --watch",
    "start:watch": "concurrently \"npm run watch\" \"node --watch dist/index.js\" \"browser-sync start --proxy localhost:8080 --files 'dist/**/*.js'\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0",
    "browser-sync": "^3.0.4",
    "concurrently": "^10.0.5",
    "node": "^26.7.0",
    "ts-node": "^10.9.2"
  }
}
